name: Build macOS Intel

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build-macos-intel:
    runs-on: macos-15-intel
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Build
        run: |
          python -m PyInstaller --clean --noconfirm spec/AstroCatalogueViewer-macos.spec

      - name: Package
        run: |
          ditto -c -k --sequesterRsrc --keepParent \
            "dist/Astro Catalogue Viewer.app" \
            "AstroCatalogueViewer-macOS-Intel.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AstroCatalogueViewer-macOS-Intel
          path: AstroCatalogueViewer-macOS-Intel.zip
